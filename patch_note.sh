sed -i 's/            summary_text, tags = await _build_summary_and_tags(/            print(f"DEBUG: calling _build_summary_and_tags for {note_id}", flush=True)\n            summary_text, tags = await _build_summary_and_tags(/g' core_api/domains/agent/core/note_utils.py
sed -i 's/        summary_text = note.summary.strip() if note.summary else ""/        print(f"DEBUG: auto_finalize_note called for {note_id}, needs_summary={needs_summary}, needs_title={needs_title}", flush=True)\n        summary_text = note.summary.strip() if note.summary else ""/g' core_api/domains/agent/core/note_utils.py
