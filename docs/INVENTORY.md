---
title: "Project Inventory — Cyberkitty119"
author: "Docs Team"
date: 2026-01-23
status: Draft
tags: [inventory, repo]
related: []
---

# Инвентаризация проекта Cyberkitty119

Документ для верхнеуровневой инвентаризации и планирования рефакторинга.

Статусы:
- `core` — текущая опорная часть системы, трогать осторожно
- `legacy` — легаси, используется по минимуму или не должен развиваться
- `dup` — дубликат / альтернативная реализация
- `remove` — кандидат на удаление после проверки

## 1. Сервисы и entrypoints

| Area     | Item / Path                                 | Role (what it does)                                  | Calls from / Entrypoints             | Status  | Problems / Risks                                                                 | Planned Actions / Refactor Steps                                                                 | Priority |
|----------|---------------------------------------------|------------------------------------------------------|--------------------------------------|---------|----------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------|----------|
| Services | `docker-compose.yml` (`bot`, `worker`, `api`, `telegram-bot-api`, `postgres`) | Описание прод-сервисов и их связей                  | Docker / prod                        | core    | Несколько compose-файлов в корне, есть следы старых флагов (DeepInfra и др.)    | Зафиксировать один «эталонный» prod-compose, остальные явно пометить dev/legacy или удалить       | M        |
| Services | `handlers.py` (корень)                      | Старый/альтернативный entrypoint бота                | Возможно не используется в прод      | legacy  | Дублирует логику из `transkribator_modules/bot/handlers.py`, может путать при поддержке | Проверить фактическое использование, если не используется — пометить как legacy и позже удалить   | M        |
| Services | `transkribator_modules/bot/handlers.py`     | Основной entrypoint Telegram-бота                    | `docker-compose.yml` сервис `bot`    | core    | Смешаны старые/новые ветки, много флагов (`AGENT_FIRST`, beta и т.п.)           | Разделить на модули по потокам (video/audio/text/agent), удалить очевидные дубли и устаревшие ветки | H        |
| Services | `job_worker.py`                             | Воркер фоновых задач                                 | `docker-compose.yml` сервис `worker` | core    | В логах видно только «plan reminders» — фактическая нагрузка по медиа не очевидна | Проинвентаризировать `job_type` и обработчики, отключить/удалить неиспользуемые джобы             | M        |
| Services | `api_server.py`                             | HTTP API / miniapp                                   | `docker-compose.yml` сервис `api`    | core    | Возможны мёртвые эндпоинты, пересечение логики с ботом                            | Составить список эндпоинтов и их реальное использование, выделить чистый API для miniapp          | M        |

## 2. Транскрипция и форматирование

| Area          | Item / Path                                                 | Role (what it does)                                                 | Calls from / Entrypoints                             | Status | Problems / Risks                                                                                                      | Planned Actions / Refactor Steps                                                                                          | Priority |
|---------------|-------------------------------------------------------------|---------------------------------------------------------------------|------------------------------------------------------|--------|-------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------|----------|
| Transcription | `transkribator_modules/transcribe/transcriber_v4.py`        | Основной пайплайн ASR (OpenRouter Gemini) + форматирование текста   | `bot/handlers.py` (`transcribe_audio`, format LLM)   | core   | Большой монолитный модуль, содержит закомментированную DeepInfra-логику и много ответвлений                            | Выделить отдельные функции: ASR, чанкинг, форматирование, заголовки; удалить/архивировать DeepInfra и неиспользуемые ветки | H        |
| Transcription | `transkribator_modules/transkribator_modules/transcribe/*`  | Дублирующий пакет транскрипции                                      | Импорты возможны из старого кода                     | dup    | Двойная структура пакетов (`transkribator_modules/.../transkribator_modules/...`), риск случайного использования легаси | Явно проверить импорты; зафиксировать один канонический путь, второй пометить как legacy и вынести из пути импорта         | H        |
| Transcription | `format_transcript_with_llm` (в `transcriber_v4.py`)        | Форматирование сырых транскрипций через OpenRouter                  | Видео/аудио-потоки, сохранение txt/docx, `notes.text` | core   | Нет защиты от артефактов типа повторяющегося «та-та-та…», модель может искажать факты и переносить контекст             | Добавить валидацию результата (повторы, сильное искажение длины/содержания), fallback на локальное форматирование          | H        |
| Transcription | `transcribe_segment_with_openrouter_gemini` (`transcriber_v4.py`) | Чанковая транскрипция через OpenRouter Gemini                       | Внутри `transcribe_whole_audio_with_gemini`          | core   | Логика ретраев и логирования есть, но нет агрегированного мониторинга по ошибкам                                       | Собрать метрики по статусам (успешно/timeout/HTTP ошибки) и добавить короткий отчёт в логи/метрики                         | M        |
| Transcription | Тесты: `test_raw_transcript.py`, `test_process_video.py`, `test_formatting.py` | Локальные сценарии проверки транскрипции и форматирования           | Локальный запуск                                     | core   | Не покрывают кейсы длинных транскриптов с артефактами и искажением смысла                                              | Добавить тесты на: длинные встречи, повторяющиеся токены, корректность дат/фактов в LLM-формате                            | M        |

## 3. LLM вокруг заметок и саммари

| Area      | Item / Path                                  | Role (what it does)                                      | Calls from / Entrypoints               | Status     | Problems / Risks                                                                                     | Planned Actions / Refactor Steps                                                                                  | Priority |
|-----------|----------------------------------------------|----------------------------------------------------------|----------------------------------------|------------|------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------|----------|
| LLM Notes | `transkribator_modules/agent/dialog.py`      | Создание заметок из транскрипта, диалоговый агент       | Видео/аудио-потоки (`ingest_and_prompt`), текстовые запросы | core       | Хранит key‑логику работы с `notes` и Google Docs, промпты зашиты в код                              | Вынести промпты и конфиг в отдельный модуль/настройки, чётко задокументировать контракт по входу/выходу           | M        |
| LLM Notes | `transkribator_modules/beta/content_processor.py` | Генерация саммари/тегов через OpenRouter                | Агент/бета-обработчики                 | core/stub  | Класс `ContentProcessor` помечен как stub; реальная логика `_build_summary_and_tags` отдельно       | Привести к единообразию: либо дописать `ContentProcessor`, либо явно использовать только `_build_summary_and_tags` | M        |
| LLM Notes | Саммари `notes.summary` (как у заметки 1494) | Краткое структурное описание встречи                     | `content_processor`, agent             | core       | Потенциальная подмена фактов (пример с датой 1 февраля, «цена слова», формализация решений)         | Ужесточить промпты (запрет выдумывать/менять факты), добавить пост‑валидацию и, по возможности, diff с исходным текстом | H        |

## 4. Telegram‑бот и диалоги

| Area    | Item / Path                                   | Role (what it does)                                  | Calls from / Entrypoints          | Status | Problems / Risks                                                                                       | Planned Actions / Refactor Steps                                                                          | Priority |
|---------|-----------------------------------------------|------------------------------------------------------|-----------------------------------|--------|--------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------|----------|
| Bot     | `transkribator_modules/bot/handlers.py`       | Основные обработчики: видео/аудио/текст, меню, agent | Сервис `bot`                      | core   | Много флагов (`AGENT_FIRST`, beta), пересечение video/audio-флоу, дубли с корневым `handlers.py`      | Разбить на подмодули (`video.py`, `audio.py`, `text.py`, `agent.py`), вычистить неиспользуемые/устаревшие обработчики | H        |
| Bot     | `transkribator_modules/transkribator_modules/bot/**` | Дублирующая версия обработчиков                      | Возможно legacy                    | dup    | Вложенный пакет может случайно попасть в импорты                                                       | Явно проверить, что он не используется; затем пометить как legacy и исключить из пути импорта                   | H        |
| Bot     | Beta‑ветки и меню (`beta/handlers`, beta-флаги) | Экспериментальные сценарии обработки                 | Бот (при включенном `FEATURE_BETA_MODE`) | mixed | Могут тянуть старые подходы, которые уже не соответствуют текущему LLM/транскрипции                   | Описать реальное использование beta, отключить/удалить мёртвые сценарии                                       | M        |

## 5. База данных и фоновые задания

| Area    | Item / Path                           | Role (what it does)                                      | Calls from / Entrypoints        | Status     | Problems / Risks                                                                                          | Planned Actions / Refactor Steps                                                                 | Priority |
|---------|---------------------------------------|----------------------------------------------------------|---------------------------------|------------|-----------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------|----------|
| DB      | Таблицы: `users`, `transcriptions`, `notes` | Основные сущности пользователей, транскрипций и заметок | Бот, API, воркер                | core       | Есть поля и таблицы под старые фичи (DeepInfra, старые планы), часть может не использоваться              | Для каждой таблицы собрать карту чтений/записей, пометить неиспользуемые поля/таблицы как кандидатов на удаление | M        |
| DB      | `processing_jobs`                    | Очередь фоновых задач                                   | `job_worker.py`, `jobs/**`      | core/mixed | По пользователю 274232565 за 2026‑01‑06 нет джоб — часть логики может обходиться без очереди             | Проверить, какие job_type реально создаются/обрабатываются, отключить/удалить неиспользуемые                     | M        |
| DB      | `transkribator_modules/db/**`        | Сервисы доступа к БД (UserService, NoteService и др.)   | Бот, API, agent, воркер         | core       | Возможны дубли/старые сервисы, неиспользуемые методы                                                     | Инвентаризировать классы/методы, удалить dead code, унифицировать работу с сессиями и транзакциями               | M        |

## 6. API / miniapp

| Area | Item / Path                         | Role (what it does)                         | Calls from / Entrypoints | Status | Problems / Risks                                                       | Planned Actions / Refactor Steps                                                          | Priority |
|------|-------------------------------------|---------------------------------------------|---------------------------|--------|------------------------------------------------------------------------|-------------------------------------------------------------------------------------------|----------|
| API  | `api_server.py`, `transkribator_modules/api/**` | HTTP API, в т.ч. для miniapp              | Сервис `api`              | core   | Неочевидно, какие эндпоинты реально используются miniapp/клиентами    | Составить список эндпоинтов и маппинг на клиентов; пометить мёртвые/deprecated маршруты   | M        |
| API  | `miniapp/**`, `miniapp_dist/**`     | UI/встраиваемое приложение                  | Через API                 | mixed  | Возможно старые сборки/артефакты, дубли фронта                        | Выяснить, какая сборка реально отдается в проде, остальные пометить как legacy или удалить | L        |

## 7. Инфраструктура, логи и тесты

| Area      | Item / Path                                       | Role (what it does)                         | Calls from / Entrypoints | Status | Problems / Risks                                                                               | Planned Actions / Refactor Steps                                                                       | Priority |
|-----------|---------------------------------------------------|---------------------------------------------|---------------------------|--------|------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------|----------|
| Infra     | `Dockerfile*`, `docker-compose.*.yml`            | Сборка и окружения                          | Docker / CI / ручной деплой | core/mixed | Много вариантов Dockerfile/compose, часть может быть историческим багажом                    | Явно классифицировать (prod/dev/test), удалить дубли и устаревшие файлы                                   | M        |
| Infra     | `setup_and_build.sh`, `deploy*.sh`, `scripts/**` | Сборка/деплой/утилиты                       | Ручной запуск             | mixed  | Есть одноразовые скрипты и старые пайплайны                                                   | Описать текущий реальный деплой, остальное пометить как legacy                                          | L        |
| Logging   | `LOGGING_IMPROVEMENTS.md`, `logs/*`, `docker_journal.log` | Стратегия логирования и диагностика       | Все сервисы               | core   | Логи не всегда содержат user_id/media_id/job_id в одном сообщении; трудно собирать полную трассу | Стандартизировать формат логов для ключевых потоков, добавить структурный контекст                        | M        |
| Tests     | `tests/**`, `test_*.py`, `test_*.sh`             | Автотесты и сценарии                        | Локально / CI             | core   | Не покрывают новые LLM‑артефакты (как «та‑та‑та»), нет регресса на искажение фактов           | Добавить тесты для длинных транскрипций, проверки формата и соответствия саммари исходному содержанию    | M        |

